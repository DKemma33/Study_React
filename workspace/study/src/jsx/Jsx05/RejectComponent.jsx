import React from 'react';

const RejectComponent = ({reject}) => {

  return (
    <div>
      <div>
        <h1>{reject}</h1>
      </div>
    </div>
  );
};

export default RejectComponent;